import React from 'react';
import { render, fireEvent } from 'react-native-testing-library';
import TodoList from '../TodoList';

describe('TodoList', () => {
  it('displays passed-in todos', () => {
    fail('Implement me!');
  });

  describe('adding a todo', () => {
    it.skip('displays the new todo', () => {
      fail('Implement me!');
    });

    it.skip('clears the text field', () => {
      fail('Implement me!');
    });
  });

  it.skip('allows deleting a todo', () => {
    fail('Implement me!');
  });
});
